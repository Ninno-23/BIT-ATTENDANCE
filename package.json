{
  "name": "attendance-otp-server",
  "version": "1.0.1",
  "description": "BIT Attendance real SMS OTP server using Semaphore",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
